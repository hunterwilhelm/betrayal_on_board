import 'package:flutter/material.dart';

class SimonSays extends StatelessWidget {
  const SimonSays({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Simon Says');
  }
}
